<UserControl x:Class="TDL.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TDL"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.6*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="0.4*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="1"
            Width="320"
            Padding="20"
            Background="White"
            CornerRadius="10"
            VerticalAlignment="Top"
            Margin="0,20,0,0">

            <StackPanel>

                <Image Source="/Images/bellasartes.ico" Height="112"/>

                <TextBlock Text="Login"
                       FontSize="26"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

               
                <TextBlock Text="Usuario" Margin="0,5"/>
                <TextBox x:Name="txtusuario"
                     Height="35"
                     FontSize="16"
                     Margin="0,0,0,10"/>

              
                <TextBlock Text="Contraseña" Margin="0,5"/>
                <PasswordBox x:Name="txtcontra"
                         Height="35"
                         Margin="0,0,0,15"/>
                

                <!-- Botón -->
                <Button Content="Acceder"
                    Height="40"
                    FontSize="16"
                    Margin="0,5"
                    Click="Acceder"
                    Background="#1E2D6B"
                    Foreground="White"
                    IsDefault="True"/>

                <Button Content="Olvide mi Contrasena"
                    Height="40"
                    FontSize="16"
                    Margin="0,5"
                    Click="Olvide"
                    Background="#1E2D6B"
                    Foreground="White"/>



            </StackPanel>
        </Border>
    </Grid>
</UserControl>

